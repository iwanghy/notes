# 并行数据结构

- 无锁链表（单链表）
- 原子交换函数
  - compare_exchange(strong weak)
  - 对比变量的值是否和expect一致，如果一致则交换新值,返回true
  - 如果不一致，将变量的值读到expect。返回false
- 三个概念
  - non-blocking
    - 一个线程卡住不会导致另一个线程被卡住
  - Lock-free
    - 一个无锁的程序并且程序总是在往前走
  - wait-free
    - 一个线程总在特定步骤后往前走